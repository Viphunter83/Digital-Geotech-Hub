# 🛠 Руководство по обслуживанию и синхронизации (Maintenance & Sync Guide)

Данный документ описывает процессы синхронизации данных между локальной разработкой и сервером (`terra-expert.ru`), а также правила внесения исправлений для предотвращения конфликтов версий.

## 🔄 Синхронизация версий

### 1. Синхронизация Исходного Кода и Git
Основной способ доставки кода на сервер — работа с Git-репозиторием.

**Локальная работа:**
1. Создайте новую ветку: `git checkout -b feature/название_фичи`
2. Сделайте коммиты: `git commit -m "описание"`
3. Отправьте в GitHub: `git push origin feature/название_фичи`
4. Создайте Pull Request (PR) и слейте его в ветку `main`.

**Обновление на Сервере:**
Все деплои происходят на сервере `155.212.209.113`.
```bash
# Подключение по SSH
export SSHPASS="PeRpWu52*f%X"
sshpass -e ssh -o StrictHostKeyChecking=no root@155.212.209.113

# Переход в директорию нужного проекта
cd /var/www/digital-geotech-hub

# Обновление из репозитория
git pull origin main

# Пересборка изменившегося контейнера (например, фронтенда)
docker compose build --no-cache frontend
docker compose up -d frontend
```

*Резервный метод (если Git недоступен):*
Синхронизация локальных изменений напрямую на сервер через `rsync`:
```bash
rsync -avz --exclude 'node_modules' --exclude '.next' --exclude '.git' ./ root@155.212.209.113:/var/www/digital-geotech-hub/
```

### 2. Синхронизация Прав Доступа (Directus Permissions)
Права доступа в Directus хранятся в БД. При добавлении новых коллекций локально, необходимо синхронизировать их с продакшном.

**Вариант А. Через API (если нет SSH-доступа):**
Для работы с API напрямую (например, из скриптов или Postman), используйте токен администратора из файла `.env` (значение `DIRECTUS_ADMIN_TOKEN` или статический токен пользователя Admin).
Авторизация передается в заголовке `Authorization: Bearer <TOKEN>`.

Пример запроса на изменение прав:
```bash
curl -X PATCH https://terra-expert.ru/directus/permissions/123 \
  -H "Authorization: Bearer ТВОЙ_ТОКЕН" \
  -H "Content-Type: application/json" \
  -d '{"fields": ["*"]}'
```

**Вариант Б. Через SSH и SQL (рекомендуемый):**
1. Выполните `python3 scripts/generate_permission_sync.py`.
2. Примените SQL на сервере:
```bash
ssh root@155.212.209.113 "cat << 'EOF' | docker exec -i geotech_db psql -U directus -d directus
$(cat scripts/sync_permissions.sql)
EOF"
```

### 3. Синхронизация Схемы Directus
Для синхронизации структуры полей и коллекций используйте JSON-снимки.
```bash
# На сервере (экспорт)
docker exec geotech_cms npx directus schema snapshot ./snapshot.json

# Локально (импорт после скачивания файла)
docker exec geotech_cms npx directus schema apply ./snapshot.json
```

**Экстренное исправление схемы (Singletons):**
Если синглтоны (например, `site_settings`) выдают `FORBIDDEN` или не имеют полей, используйте SQL-фикс:
```bash
ssh root@155.212.209.113 "docker exec -i geotech_db psql -U directus -d directus" < scripts/fix_schema.sql
docker restart geotech_cms
```

---

## 🛠 Инструкции по исправлениям на сервере

### Пересборка контейнеров (Решение проблем с кэшем)
При изменении исходного кода (в особенности фронтенда на Next.js) всегда выполняйте чистую сборку. **🚨 КРИТИЧЕСКИ ВАЖНО:** Из-за агрессивного кэширования Next.js одного лишь `docker compose build --no-cache` может быть недостаточно, если на сервере в рабочей директории остались старые папки `.next`.

Добавлен `.dockerignore` файл, но для гарантии 100% чистой сборки используйте следующий фрагмент (выполняется внутри сервера по SSH):
```bash
# Подключение: export SSHPASS="PeRpWu52*f%X" && sshpass -e ssh -o StrictHostKeyChecking=no root@155.212.209.113

# На сервере:
cd /var/www/digital-geotech-hub/frontend
rm -rf .next node_modules # Удаление паразитного кэша на хосте
cd ..
docker compose build --no-cache frontend
docker compose up -d frontend
```

### Проверка логов
```bash
docker logs -f geotech_frontend   # Фронтенд (Next.js)
docker logs -f geotech_cms        # Directus
docker logs -f geotech_gateway    # Nginx
```

### Перезагрузка Nginx (Gateway)
Если вы изменили `nginx/default.conf`:
```bash
docker exec geotech_gateway nginx -s reload
```

⚠️ **Внимание**: В `default.conf` используйте уникальные имена контейнеров (`geotech_api`, `geotech_cms`), так как на сервере могут быть другие проекты в той же сети. Не используйте переменные в `proxy_pass`, если они обрезают пути.

---

## ⚠️ Предотвращение конфликтов (Best Practices)

1. **Единственный источник правды**: Все изменения в схеме данных (колонки, типы) должны сначала вноситься локально, фиксироваться в `snapshot.json` и только потом деплоиться на сервер.
2. **Переменные окружения**: Никогда не храните секреты в `docker-compose.yml` или скриптах. Все пароли и SSH-ключи должны загружаться из `.env`. В продакшне `FRONTEND_URL` и `BACKEND_URL` должны указывать на `https://terra-expert.ru`.
3. **PUBLIC_URL & Paths**: При настройке путей Directus всегда проверяйте, что `PUBLIC_URL` указывает на `https://terra-expert.ru/directus/`. В Nginx `proxy_pass` для API должен передавать полный путь.
4. **Регистр иконок**: В CMS и коде используйте строгий маппинг через `resolveIcon` (см. `frontend/src/lib/icon-map.ts`). Избегайте использования строк `icon_name` напрямую без нормализации.
5. **Координаты**: Все гео-данные в Directus должны быть валидными числами. Компонент `ProjectsMap` отфильтровывает `null`-значения, но рекомендуется чистить базу от "мусора".

---

## 🏗 Мониторинг и Отработка ошибок
- **502 Bad Gateway**: Обычно означает, что контейнер `frontend` или `directus` упал или находится в процессе перезагрузки.
- **Client-Side Exception (Hydration mismatch)**: Проверьте консоль браузера. Чаще всего причина в несоответствии данных из-за кэширования. Выполните Hard Reload (Ctrl+F5).
- **Сайт показывает старую версию (Browser Cache)**: Если код обновился и контейнеры перезапущены, но клиент видит старую версию — попросите выполнить Hard Reload (Ctrl+F5) в браузере или очистите кэш Next.js (см. раздел "Пересборка контейнеров").
