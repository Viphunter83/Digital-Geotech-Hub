# 🏗 Architecture: Terra Expert

## 🌟 Concept
**Terra Expert** — это B2B-платформа для строительной отрасли (геотехника), которая автоматизирует пресейл через AI-аудит техзаданий и предоставляет интерактивный каталог решений.

---

## 🛠 Технологический стек

### Frontend (Next.js 15+ App Router)
- **UI/UX**: Tailwind CSS, Framer Motion (анимации), Lucide React.
- **Масштабируемость**: Использование Client Components для интерактивных вставок (`InteractiveMap.tsx`) в серверных страницах.
- **Картография**: Leaflet для визуализации объектов с кастомным бруталистичным фильтром.
- **Интеграция**: Directus SDK + Fetch API с кастомными fallback-слоями.

### Backend (FastAPI + Python 3.14)
- **AI Core**: 
  - `llm.py`: Двухэтапный промптинг (JSON-структура + Markdown-резюме) через GPT-4o.
  - Роль AI: **Senior Geotechnical Engineer**.
- **Data Integration**:
  - `directus.py`: Сервис сопоставления извлеченных данных AI с базой материалов (шпунтов) и техники.

### Production Infra & CI/CD
- **GitHub Actions**: Автоматизированный пайплайн сборки и деплоя. При пуше в `main` образы пушатся в **GHCR (GitHub Container Registry)**, после чего инициируется обновление на сервере через SSH.
- **Reverse Proxy**: **Nginx** (Gateway) в контейнере `geotech_gateway`. Использует **изолированные имена апстримов** (`geotech_api`, `geotech_cms`) для предотвращения конфликтов с соседними проектами в Docker-сети.
- **Routing Strategy**: Настроен прозрачный прокси-пасс (Transparent Proxying) для сохранения путей `/api/v1/...`, что исключает 404 ошибки на FastAPI.
- **Security**: **Let's Encrypt** SSL-сертификаты. Directus защищен постоянным Bearer-токеном.
- **Directus 11.2**: Централизованное управление контентом.
- **PostgreSQL 17**: Реляционная база данных.
- **Redis 7**: Кэширование и управление очередями.
- **Data Recovery**: Использование `directus_schema_snapshot.json` для миграции структуры и скриптов сидирования для восстановления данных.

---

## 🔄 Потоки данных и Логика

### 1. AI Technical Audit (Dashboard-First v2.0)
Вместо обычного чата система использует **Dashboard-First** подход, предоставляя структурированные данные сразу после загрузки.

**Pipeline анализа:**
1.  **High-Fidelity Extraction**: `DocumentProcessor` выполняет парсинг PDF/XLSX с учетом разметки (Layout Aware), сохраняя таблицы и структуру разделов (Геология, Технические решения).
2.  **Geotechnical RAG Layer**: AI обращается к локальной базе стандартов (`geotech_standards.json`), включающей пункты из ГОСТ 25100 и СП 45.13330. Это гарантирует точность выводов и соответствие нормативам.
3.  **Multi-Pass Inference**: 
    - **Pass 1**: Извлечение геометрии и геологии.
    - **Pass 2**: Оценка рисков на основе извлеченных данных и стандартов.
    - **Pass 3**: Генерация экспертного резюме в стиле Senior Engineering Lead.
➔ Backend сопоставляет данные с каталогом Directus ➔ Frontend отображает детальный отчет с матрицей рисков.

### 2. Geological Intelligence Layer
Карта содержит два независимых слоя:
- **Projects**: Реальные кейсы из Directus.
- **Geology**: Геологический контекст регионов (СПБ/МСК) с рекомендациями по методам погружения.

### 3. Hyper-personalization Engine (GEO-Aware)
Система адаптирует контент под регион пользователя (Москва/СПб) на лету:
- **Detection**: `middleware.ts` анализирует URL-параметр `?geo=...` или куки `x-geo-region`.
- **Injection**: Регион прокидывается в `Hero`, `InteractiveMap` и `ServiceFAQ`.
- **Content Strategy**:
    - **СПб**: Акцент на деликатном погружении в историческом центре (Silent Piler).
    - **МСК**: Акцент на скорости перебазировки и масштабах свайных полей.

### 5. Content Management Architecture (Singleton-Based)
Для обеспечения удобства и надежности используется система синглтонов:
- `company_info`: Единый источник правды для контактов по всему сайту.
- `site_settings`: Управление SEO-тегами (Title/Description) без изменения кода.
- `about_page`: Тексты миссии и истории, управляемые через Rich Text Editor.

### 6. Relational Data Flow (Projects + Machinery)
Реализована M2M связь `projects_machinery`. Фронтенд при запросе проекта через `fetchProjectById` автоматически подгружает данные о связанных машинах из каталога, обеспечивая целостность информации. Из-за наличия как ручных, так и реляционных данных, на фронтенде работает **Smart Merge Logic** (см. `projects-data.ts`), который дедуплицирует записи по именам.

### 7. AI Calculator Intelligence Layer (Advanced Estimations)
Итоговая сумма в КП рассчитывается по формуле:
`Итого = (Материалы + Работы + Аренда техники) * Коэффициент сложности`

*   **Материалы**: Вес шпунта из ТЗ × Цена из закрытой коллекции `shpunts` (Stock data).
*   **Работы**: Объем из ТЗ × Ставки из `site_settings` (Singleton).
*   **Аренда техники**: Ставка за смену из `machinery` × Кол-во смен (оценивает AI).
*   **Коэффициент сложности**: AI (GPT-4o) анализирует ТЗ на наличие рисков (стесненность, болото) и применяет множитель 1.0–1.5.

### 8. Operational Stability Layer (NEW)
Для соответствия высоким стандартам надежности внедрены:
- **Resilience**: Swap-файл (2ГБ) защищает от OOM-падений БД и Node.js.
- **Disaster Recovery**: Скрипт `backup_db.sh` + Cron ежедневно создают инкрементальные бэкапы.
- **Security Headers**: Nginx терминирует SSL и добавляет заголовки HSTS/CSP.

---

## 📂 Структура папок
- `frontend/src/components/layout`: Основные блоки UI (Map, Hero, AI Audit).
- `backend/app/api/v1/endpoints`: Эндпоинты AI и сопоставления данных.
- `backend/app/services`: Ядро логики (LLM, Directus Service).
- `infrastructure/`: Конфигурации Docker.
- `scripts/`: Автоматизация (Бэкапы, Синхронизация прав, Полиш UI).

---

## 🎨 Дизайн-принципы
- **Business-Ready CMS**: Directus UI сгруппирован по папкам `[КОНТЕНТ]`, `[КАТАЛОГ]`, `[БИЗНЕС]`, `[СИСТЕМА]`.
- **Brutalist Dark**: Использование инвертированных карт, строгих сеток и Safety Orange (#F97316) в качестве акцента.
- **Typography**: Inter (инженерная четкость) + Outfit (премиальный вид).
