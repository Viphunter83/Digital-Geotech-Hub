# 📋 Handover Guide: Digital Geotech Hub

Этот документ предназначен для разработчика, который продолжит работу над проектом.

## 🔑 Доступы и Ссылки
- **Directus Admin**: `http://localhost:8055`
  - Login: `admin@geotech.hub`
  - Pass: `geotech-hub-2026`
- **FastAPI Docs**: `http://localhost:8000/docs` (Swagger)
- **Frontend**: `http://localhost:3000`

## 🛡️ Особенности реализации: "Demo Resilience"
Проект находится в состоянии **презентационной готовности**. 
1. **Fallbacks**: В коде фронтенда (`ProjectsMap.tsx`, `ServiceFAQ.tsx`) и бэкенда (`directus.py`) реализованы жесткие fallback-данные. Если API Directus не отвечает, система подставляет качественные заглушки.
2. **AI Mocking**: В `llm.py` и `ai_copilot.py` настроена логика, которая при сбоях API возвращает заранее подготовленные ответы, чтобы презентация не прервалась.

## 🛠️ Как продолжить разработку
1. **Наполнение данными**: В данный момент база Directus содержит минимальный набор данных. Для полноценной работы необходимо заполнить коллекции `cases`, `machinery`, `services` и `shpunts`.
2. **Environment Synchronization**: Убедитесь, что `.env` (backend) и `.env.local` (frontend) синхронизированы. Ключевая переменная — `NEXT_PUBLIC_DIRECTUS_URL`.
3. **AI Tuning**: В `backend/app/services/llm.py` можно уточнять промпты для `Senior Geotechnical Engineer`. Сейчас используется GPT-4o.

## ⚠️ Известные нюансы
- **Hydration**: На `<html>` теге установлен `suppressHydrationWarning`. Это необходимо для корректной работы в средах, внедряющих свои атрибуты (например, Jetski или браузерные расширения).
- **Leaflet SSR**: Интерактивная карта обернута в `InteractiveMap.tsx` (Client Component) для предотвращения ошибок SSR в Next.js App Router.

## 📈 Roadmap
- [ ] Перевод всех компонентов на 100% получение данных из Directus и отключение Fallbacks.
- [ ] Доработка калькулятора смет в `ai_copilot.py` для учета рыночных цен из Directus.
- [ ] Настройка полноценного Auth-модуля для личных кабинетов B2B-клиентов.

---
Удачи! Продукт имеет большой потенциал в автоматизации скучных расчетов для инженеров.
