# 🛡️ ПРЕДОСТОРОЖНОСТЬ: Конфигурация MCP

> [!WARNING]
> **ВНИМАНИЕ**: Инструменты `directus-mcp` в данный момент подключены к другому проекту (обнаружены коллекции: `amocrm_settings`, `client_equipment`, `products` и др.).
> 
> **ДЛЯ DIGITAL GEOTECH HUB MCP НЕ НАСТРОЕН.**

---

## 🚫 Ограничения
До момента перенастройки MCP:
1. **НЕ ИСПОЛЬЗОВАТЬ** инструменты `mcp_directus-mcp_*` для записи данных (create, update, delete).
2. **ИГНОРИРОВАТЬ** коллекцию `products` / `articles` при работе с этим проектом, так как они относятся к другой системе.
3. Все операции с Directus проводить исключительно через `directus.ts` (Next.js SDK) или ручные API-запросы, если это необходимо.

---

## 🛠 Инструкция по перенастройке (для пользователя)
Чтобы я мог безопасно работать с Directus этого проекта через MCP, обновите конфигурацию MCP сервера:

- **Адрес**: `http://localhost:8055`
- **Email**: `admin@geotech.hub`
- **Пароль**: `geotech-hub-2026` (взято из PROJECT_STATE.md)

---

## ✅ Фиксация
Этот файл является "виртуальным предохранителем". Я (ИИ) обязуюсь проверять наличие этого файла при попытке использования MCP инструментов.
